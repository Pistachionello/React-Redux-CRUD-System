const usersListString = "/users_list";
const carsListString = "/cars_list";
const defaultString = "/";

export const ROUTES = {
    usersList: usersListString,
    carsList: carsListString,
    DEFAULT: defaultString,
}